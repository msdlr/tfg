pragma solidity >=0.4.22 <0.7.0;

contract AuthContract {

    // This contract is instantiated by every person who has
    // to login
    constructor(address _gContract, address _employee) public payable{

    }
}